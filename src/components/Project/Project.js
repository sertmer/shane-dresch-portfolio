import './Project.css';

export default function Project({id, title, videoLink, embedLink, stills, aspectRatio}) {
  console.log(id, title)
  return (
    <div>
      <p>{id}</p>
      <p>hello</p>
    </div>
  )

}